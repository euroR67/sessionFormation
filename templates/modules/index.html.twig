{% extends 'base.html.twig' %}

{% block title %}Liste des modules{% endblock %}

{% block body %}

    <h1>Liste des modules</h1>

    <table>
        <thead>
            <tr>
                <th>Nom</th>
                <th>Catégorie</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            {% for module in modules %}
                <tr>
                    <td>{{ module }}</td>
                    <td>{{ module.categories }}</td>
                    <td>
                        <a href="{{ path('edit_modules', {'id': module.id}) }}">Editer</a><br>
                        <a href="{{ path('delete_modules', {'id': module.id}) }}">Supprimer</a><br>
                    </td>
                </tr>
            {% endfor %}
        </tbody>

{% endblock %}
